# 阶段 7 页面装修与拼团开发执行计划

> 设计依据：`docs/superpowers/specs/2026-08-18-phase7-decoration-groupbuy-design.md`

## 任务 1：数据库与实体

- 新增 `sql/mysql/biz_phase7.sql`，创建装修与拼团 6 张表和约束。
- 新增 Decoration、GroupBuy 实体与状态常量。
- 注册两个业务模块并运行后端构建。

## 任务 2：页面装修服务端

- 增加 Banner、公告、品牌 DTO、Service、B 端 Controller。
- 增加公开的 C 端首页聚合接口。
- 复用现有分页响应、文件上传和 RBAC，不新增依赖。

## 任务 3：拼团订单与生命周期

- 在 `OrderService` 中增加固定拼团价订单创建入口，订单优惠字段固定为零。
- 导出现有领域事件总线，拼团服务订阅支付、取消和退款事件。
- 实现活动 CRUD、发起团、参团、成团、取消释放和查询接口。
- 实现每分钟状态补偿、超时失败和幂等退款重试。

## 任务 4：后端测试与构建

- 增加拼团活动校验、名额、支付成团、取消释放、超时退款测试。
- 运行阶段 7 测试、全量 Jest、lint 和生产构建。

## 任务 5：菜单、测试数据与本地数据库

- 新增 `sql/mysql/menu_phase7.sql`，显式使用 `utf8mb4` 并授权 ROOT、ADMIN。
- 更新 `sql/mysql/biz_test_seed.sql`，增加装修和拼团场景。
- 将阶段 7 SQL 应用到本地 MySQL，并清理角色权限缓存。

## 任务 6：管理端

- 新增装修和拼团 API 类型与调用封装。
- 新增 Banner、公告、品牌和拼团管理页面。
- 复用现有表格、抽屉、上传和富文本模式，不改系统层页面。
- 运行类型检查、生产构建和代码规范检查。

## 任务 7：闭环验收与交付

- 验证装修 C 端聚合接口和 B 端 CRUD。
- 验证发起团、参团、Mock 支付、成团、取消和超时退款。
- 在管理端验证全部菜单、筛选、表单和详情，无乱码、无 403。
- 更新三端进度说明；小程序仍标记为未接入。
- 后端与管理端分别提交，保留用户原有未提交文件。
