# 预约时段容量实施计划

1. 扩展阶段 6 SQL，增加单行预约容量配置及管理权限。
2. 服务端增加配置实体、配置接口、固定时段查询和事务内容量校验。
3. 补充预约服务最小测试，覆盖固定时段、满额、容量配置和时段状态。
4. 管理端预约页增加统一容量读取与保存。
5. 更新预约产品文档，执行服务端测试/构建与管理端类型检查/构建。

小程序代码不在本次实施范围，待办以设计文档和小程序改造计划为准。
