# 仪表盘待办顺序修复设计

## 问题

待办查询当前按 `occurredAt ASC` 排序并限制 5 条，因此返回的是最旧待办，而不是最新待办。

## 修复

将后端待办查询改为 `ORDER BY occurredAt DESC LIMIT 5`。前端保持按接口顺序渲染，不增加排序参数或二次排序。

## 验证

- 聚合接口返回最多 5 条待办。
- 相邻待办的 `occurredAt` 按从新到旧排列。
- 后端测试与构建通过。
